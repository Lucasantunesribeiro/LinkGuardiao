services:
  api:
    image: ${GHCR_IMAGE:-ghcr.io/your-user/linkguardiao-api:latest}
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__Default: ${CONNECTIONSTRINGS__DEFAULT}
      Jwt__Secret: ${JWT__SECRET}
      Jwt__Issuer: ${JWT__ISSUER:-LinkGuardiao}
      Jwt__Audience: ${JWT__AUDIENCE:-LinkGuardiao}
      Cors__AllowedOrigins__0: ${CORS__ALLOWEDORIGINS__0}
      LinkLimits__DailyUserCreateLimit: ${LINKLIMITS__DAILYUSERCREATELIMIT:-100}
      Seed__Enable: "false"
    ports:
      - "8080:8080"
    volumes:
      - linkguardiao_data:/var/lib/linkguardiao

volumes:
  linkguardiao_data:
